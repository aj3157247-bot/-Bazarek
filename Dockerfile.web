# Website-only deployment: serve the independent HTML/CSS/JS storefront.
# Flutter Android / GitHub Actions APK build is not changed.
FROM nginx:alpine
COPY web/ /usr/share/nginx/html/
COPY nginx.conf /etc/nginx/nginx.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
